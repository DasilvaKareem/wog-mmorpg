{
  "installCommand": "cd docs && npm install && cd ../client && npm install",
  "buildCommand": "cd docs && npm run build && cd ../client && npm run build && cp -r ../docs/dist ./dist/docs",
  "outputDirectory": "client/dist",
  "framework": null,
  "env": {
    "VITE_API_URL": "https://private-accessible-asia-qualified.trycloudflare.com"
  },
  "build": {
    "env": {
      "VITE_API_URL": "https://private-accessible-asia-qualified.trycloudflare.com"
    }
  }
}
